# Base de données Neon (PostgreSQL) — créer sur https://neon.tech
DATABASE_URL=postgresql://user:password@host/pharmaguard?sslmode=require

# Session secrète — générer avec: openssl rand -base64 32
SESSION_SECRET=remplacer_par_une_chaine_secrete_de_32_chars_minimum

# URL de l'app (Vercel la remplit automatiquement)
NEXT_PUBLIC_APP_URL=https://ton-app.vercel.app
